{
  "name": "@cv-validation-app/shared",
  "version": "1.0.0",
  "description": "Shared types and schemas for CV validation application",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "zod": "^4.0.5"
  }
}
